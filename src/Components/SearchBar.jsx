import React from 'react'

const SearchBar = () => {
  return (
    <div>
        <input type="search"
        className='input'
        placeholder='Search by name...' />
    </div>
  )
}

export default SearchBar